

# This config is a STUB! This should never be generated.
# Use the default lua config from https://github.com/hyprm/Hyprland/blob/main/example/hyprland.lua

autogenerated = 1

$mainMod = SUPER
$terminal = kitty
$fileManager = thunar
$menu = hyprlauncher

bind = $mainMod, Q, exec, $terminal
bind = $mainMod, C, killactive,
# bind = $mainMod, M, exec, command -v hyprshutdon >/dev/null 2>&1 && hyprshutdown || hyprctl dispatch exit
bind = $mainMod, E, exec, thunar,
bind = $mainMod, V, togglefloating,
bind = $mainMod, R, exec, $menu
